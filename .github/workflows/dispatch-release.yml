name: Dispatch release

# A pushed version tag asks the private release pipeline to build, sign and
# notarize it. That pipeline publishes the result back here as a draft release.

on:
  push:
    tags: ['v*.*.*']

permissions: {}

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Request a release build
        env:
          GH_TOKEN: ${{ secrets.RELEASE_DISPATCH_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "Not a release tag: $TAG"; exit 0; }
          gh api repos/TouchBitsInc/craft-release/dispatches \
            -f event_type=release -f "client_payload[tag]=$TAG"
          echo "Requested a release build of $TAG"
